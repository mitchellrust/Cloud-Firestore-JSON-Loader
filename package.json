{
  "name": "cloud_firestore_json_loader",
  "version": "1.0.0",
  "description": "This is to upload JSON to Firestore",
  "main": "uploader.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "RetroPortal Studio",
  "license": "ISC",
  "dependencies": {
    "firebase": "^6.6.2",
    "firebase-admin": "^8.9.2"
  }
}
